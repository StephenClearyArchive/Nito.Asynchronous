This is a completely blank, default ASP.NET web application.

It exists only so that the FrameworkAssumptions test project can execute ASP.NET-hosted tests.

The actual class under test is AspNetSynchronizationContext, which is provided by the ASP.NET host when the tests are run.